## Getting started

1. Clone this repository
2. Run `npm install`

    ### iOS
      
    - Run `pod install` from `EPWA-RN/ios` folder
    -  `npm ios` will open xCode 
    - build from xCode
    
    ### Android
    
    - Run `npm android`