import { StyleSheet } from "react-native";

export default StyleSheet.create({
  formContainer: {
    justifyContent: "center",
    alignItems: "center"
  },
  fieldContainer: {
    width: 240
  },
  button: { minWidth: 240 }
});
