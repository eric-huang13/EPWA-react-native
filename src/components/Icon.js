import { createIconSetFromIcoMoon } from "react-native-vector-icons";

import icoMoonConfig from "../icons/config.json";

const Icon = createIconSetFromIcoMoon(icoMoonConfig, "icomoon");

export default Icon;
